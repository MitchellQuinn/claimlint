# Output Examples

Each `claims.jsonl` line is one claim record matching `schemas/claim_record.schema.json`.

For no-claim runs, `claims.jsonl` is empty and Markdown reports explicitly state that no substantial auditable repository claims were found in the selected corpus.

