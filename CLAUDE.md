@AGENTS.md

# Claude Code notes

Use the ClaimLint skill if installed.

The canonical workflow is `workflows/claim_audit.yml`.
The runtime contract is `docs/runtime_contract.md`.

Do not treat this file as the workflow source.

